//load scripts via browserify
global.jQuery = require('jquery');
bootstrap = require('bootstrap');
mustache = require('mustache');

//web specific scripts
jQuery(document).ready(function(){
  //code
});
